{
  "name": "json-server-exam",
  "version": "1.0.0",
  "scripts": {
    "start": "json-server --watch pokemon.json --port 4000 --read-only --routes routes.json --static ./assets" 
  },
  "dependencies": {
    "json-server": "^0.17.3"
  }
}
